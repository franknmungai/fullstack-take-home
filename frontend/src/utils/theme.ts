import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Mulish',
  },
  palette: {
    primary: {
      main: '#4AA088', //'#5ACCCC',
    },
    secondary: {
      main: '#F76434',
    },
  },
});

export default theme;
